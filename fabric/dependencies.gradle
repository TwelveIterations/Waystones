dependencies {
    modImplementation(libs.balmFabric) {
        changing = libs.versions.balm.get().contains("SNAPSHOT")
    }
    modCompileOnly(libs.journeyMapApiCommon)
    modCompileOnly(libs.blueMap)
    modCompileOnly(libs.dynmap)
    modCompileOnly(libs.unbreakablesCommon)
}